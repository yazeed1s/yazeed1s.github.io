<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <title>Yazeed&#39;s Blog - OS</title>
    <subtitle>Notes on systems and low-level software.</subtitle>
    <link rel="self" type="application/atom+xml" href="https://yazeed1s.github.io/tags/os/atom.xml"/>
    <link rel="alternate" type="text/html" href="https://yazeed1s.github.io"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>2026-02-16T00:00:00+00:00</updated>
    <id>https://yazeed1s.github.io/tags/os/atom.xml</id>
    <entry xml:lang="en">
        <title>Should malloc Know About Tiered Memory?</title>
        <published>2026-02-16T00:00:00+00:00</published>
        <updated>2026-02-16T00:00:00+00:00</updated>
        
        <author>
          <name>Unknown</name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://yazeed1s.github.io/posts/malloc-to-teired/"/>
        <id>https://yazeed1s.github.io/posts/malloc-to-teired/</id>
        
        <content type="html" xml:base="https://yazeed1s.github.io/posts/malloc-to-teired/">&lt;p&gt;This started as a side thought during advanced systems class, we were talking about OS transparency in general and I kept coming back to malloc specifically afterward, because it&#39;s the one abstraction I interact with the most and never actually question. So this is me chewing on it after the fact, not a fully formed argument.&lt;/p&gt;
&lt;p&gt;When you call &lt;code&gt;malloc()&lt;/code&gt;, the allocator gives you a pointer. It doesn&#39;t know or care whether the physical page behind it sits in fast local DRAM or slower CXL-attached memory, and it does not even care if hugepages are enabled or not, and from user space memory still looks completely flat, one address space, no notion of near or far. Except it isn&#39;t flat anymore, not really. Machines now have 2-3x latency differences between memory tiers and the allocator is completely blind to all of it.&lt;/p&gt;
&lt;p&gt;glibc&#39;s allocator (ptmalloc2) was built for a mostly uniform DRAM world. It manages arenas, splits and coalesces chunks, decides when to use &lt;code&gt;brk&lt;/code&gt; vs &lt;code&gt;mmap&lt;/code&gt;, worries about lock contention between threads, all of that. What it does not worry about is which NUMA node ends up backing an allocation, unless you explicitly ask it to. In the common path it just requests virtual memory and leaves the physical placement decision entirely to the kernel. Memory, from the allocator&#39;s point of view, is virtual address space and nothing else, it has no idea if the physical pages come from local DRAM, remote NUMA, CXL, or somewhere weirder. That was fine when latency gaps were small and mostly a bandwidth-balancing story, and it&#39;s not really fine anymore.&lt;/p&gt;
&lt;p&gt;Tiered memory systems now treat slow memory as basically another NUMA node, demoting cold pages down and promoting hot ones back up when they get busy again. TPP migrates based on observed access frequency, and Memtis goes further and looks at access distribution, even splitting huge pages when the access pattern inside one is skewed. But notice the pattern is always the same shape, you allocate first, observe later, and migrate if needed, so we&#39;re always reacting.&lt;/p&gt;
&lt;p&gt;And migration is not free, not even close, since you&#39;re copying 4KB pages, updating page tables, invalidating TLB entries, and maybe disturbing caches on top of it. M5&#39;s work shows misclassification and migration overhead can actively hurt performance if you&#39;re not careful with it. So you&#39;re paying a correction tax because the initial allocation was made blind. I keep wondering how much of that tax disappears if the allocator just knew &lt;em&gt;something&lt;/em&gt; about what&#39;s hot before it even placed the memory.&lt;/p&gt;
&lt;p&gt;take this:&lt;/p&gt;
&lt;pre class=&quot;giallo z-l-code z-d-code&quot; &gt;&lt;code data-lang=&quot;c&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-4 z-d-8&quot;&gt;void&lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt; *&lt;/span&gt;&lt;span&gt;hot_table &lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt; malloc&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;z-l-4 z-d-5&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt; &amp;lt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;z-l-4 z-d-5&quot;&gt; 20&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;;&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt;   //&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt; frequently accessed&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-4 z-d-8&quot;&gt;void&lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt; *&lt;/span&gt;&lt;span&gt;log_buffer &lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt; malloc&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;z-l-4 z-d-5&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt; &amp;lt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;z-l-4 z-d-5&quot;&gt; 20&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;;&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt;  //&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt; rarely accessed&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-4 z-d-8&quot;&gt;void&lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt; *&lt;/span&gt;&lt;span&gt;archive &lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt; malloc&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;z-l-4 z-d-5&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt; &amp;lt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;z-l-4 z-d-5&quot;&gt; 20&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;;&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt;   //&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt; mostly cold&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;identical calls as far as glibc is concerned. same api, same path, no distinction whatsoever. but the temperature of these three allocations is wildly different and the allocator has zero way to express or even detect that. the application usually already knows this stuff though. a database knows its buffer pool is hot. a web server knows which structures sit in the request path. a compiler knows which tables get reused constantly. and yet we throw all of that away and make the kernel re-derive it from access bits and sampling, from scratch, every time.&lt;/p&gt;
&lt;p&gt;so are we solving this problem too late in the pipeline? maybe.&lt;/p&gt;
&lt;p&gt;one idea, not that exotic honestly, is to let the allocator take intent as input.&lt;/p&gt;
&lt;pre class=&quot;giallo z-l-code z-d-code&quot; &gt;&lt;code data-lang=&quot;c&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-4 z-d-8&quot;&gt;void&lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt; *&lt;/span&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt;malloc_hot&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;z-l-4 z-d-8&quot;&gt;size_t&lt;/span&gt;&lt;span class=&quot;z-l-6&quot;&gt; size&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;;&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-4 z-d-8&quot;&gt;void&lt;/span&gt;&lt;span class=&quot;z-d-6&quot;&gt; *&lt;/span&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt;malloc_cold&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;z-l-4 z-d-8&quot;&gt;size_t&lt;/span&gt;&lt;span class=&quot;z-l-6&quot;&gt; size&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;z-d-1&quot;&gt;;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;malloc_hot()&lt;/code&gt; could bind to the fast NUMA node using stuff that already exists like &lt;code&gt;mbind()&lt;/code&gt; and &lt;code&gt;set_mempolicy()&lt;/code&gt;, nothing new needed under the hood, and &lt;code&gt;malloc_cold()&lt;/code&gt; allocates straight onto the slow tier. Instead of allocate then detect then migrate you just allocate correctly the first time, which skips a bunch of migration outright, means fewer TLB shootdowns and less copying, and makes placement something you decide instead of something that gets corrected later.&lt;/p&gt;
&lt;p&gt;which brings up the actual question underneath all of this. is OS transparency still sacred here?&lt;/p&gt;
&lt;p&gt;virtual memory exists specifically to hide physical placement, that&#39;s the whole point of it, developers don&#39;t need to know or care where their bytes physically live, they just allocate and use. tiered memory pokes at that. once the latency gap gets big enough placement starts mattering again whether we like it or not.&lt;/p&gt;
&lt;p&gt;you could keep full transparency, kernel infers temperature from access patterns, developers stay fully insulated, and the system just grows more internal complexity to deal with it, more sampling, more migration machinery. or you leak some of the abstraction, let developers label things hot or cold and trust them, let the allocator actually participate in placement decisions.&lt;/p&gt;
&lt;p&gt;I genuinely don&#39;t know which is better. the second one sounds cleaner right up until you think about what happens when people misclassify things. what happens when everyone just labels everything &quot;fast&quot; because why not. do you override the hint? ignore it silently? once you expose placement you also hand out responsibility for it, and I don&#39;t think most application developers actually want that responsibility, even the ones who complain about performance.&lt;/p&gt;
&lt;p&gt;huge pages make this whole thing worse, by the way, almost forgot to mention it. Memtis shows access inside a single 2MB huge page can be extremely skewed, so promoting the entire page because a tiny sliver of it is hot wastes a lot of fast-tier capacity for nothing. the allocator has no idea how its own allocations line up against huge page boundaries, and the kernel might split or merge those pages on you later anyway. page size, allocation strategy, and tier placement are all tangled together now and I don&#39;t think anyone has a clean model for how they&#39;re supposed to interact. the old layering assumed these were independent concerns. they are not, not anymore.&lt;/p&gt;
&lt;p&gt;I don&#39;t think transparency should get abandoned entirely, it&#39;s still genuinely valuable for the vast majority of applications that don&#39;t care about squeezing out this kind of performance. but maybe it doesn&#39;t have to be all-or-nothing. default stays abstract, kernel handles it like today, and for the performance-critical stuff the allocator exposes controlled hints while the kernel keeps some override power so a bad hint doesn&#39;t destabilize the whole system.&lt;/p&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>A Buffer Pool Is Just Paging in User Space</title>
        <published>2025-12-02T00:00:00+00:00</published>
        <updated>2025-12-02T00:00:00+00:00</updated>
        
        <author>
          <name>Unknown</name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://yazeed1s.github.io/posts/buffer-pools/"/>
        <id>https://yazeed1s.github.io/posts/buffer-pools/</id>
        
        <content type="html" xml:base="https://yazeed1s.github.io/posts/buffer-pools/">&lt;p&gt;A database buffer pool manages fixed-size pages in memory, decides which ones to keep and which to evict, tracks dirty pages, and writes them back to disk on its own schedule. That&#39;s the same thing the OS virtual memory system does with page frames, page tables, eviction policies, dirty bit tracking, and write-back, and the database reimplements all of it in user space on purpose.&lt;/p&gt;
&lt;h2 id=&quot;the-os-already-does-this&quot;&gt;the OS already does this&lt;/h2&gt;
&lt;p&gt;The kernel manages physical memory in 4KB page frames and maps virtual pages to physical frames through page tables, and when memory is full it evicts cold pages to disk, so when a process touches an evicted page it faults and the kernel loads it back, and it tracks which pages are dirty and writes them back when it needs to. This is the exact same problem a database has, where it has pages on disk, some of them need to be in memory, not all of them fit, and it has to decide which pages to keep, which to evict, and when to write the dirty ones back.&lt;/p&gt;
&lt;p&gt;So why not just let the OS handle it, map the database file with mmap and let the kernel manage everything? Some databases tried this and &lt;a href=&quot;https://yazeed1s.github.io/posts/mmap-databases/&quot;&gt;it didn&#39;t go well&lt;/a&gt;.&lt;/p&gt;
&lt;h2 id=&quot;why-databases-reimplement-it&quot;&gt;why databases reimplement it&lt;/h2&gt;
&lt;p&gt;The OS page cache is general purpose, so it has no concept of index pages versus temporary sort pages, no awareness that a range scan is about to need the next 50 pages, and no understanding that a dirty page has to hit the WAL before it hits the data file, and a buffer pool knows all of that.&lt;/p&gt;
&lt;p&gt;The database builds its own page table, a hash map from &lt;code&gt;(file_id, page_number)&lt;/code&gt; to a frame in the buffer pool, so when a query needs a page it looks up the hash map, and if the page is there it returns the pointer, and if not it picks a frame to evict, reads the page from disk into that frame, and updates the map. It&#39;s a page fault, just in user space and controlled entirely by the database.&lt;/p&gt;
&lt;h2 id=&quot;the-anatomy-of-a-buffer-pool&quot;&gt;the anatomy of a buffer pool&lt;/h2&gt;
&lt;p&gt;The structure is simple. There&#39;s a frame array, a fixed-size array of page-sized slots in memory that acts as the RAM of the buffer pool. There&#39;s the page table, a hash map from page ID to frame index that translates a logical page reference into a memory location. There&#39;s an eviction policy like LRU, clock, or LRU-K that decides which frame to reclaim when the pool is full. Each frame has a dirty flag tracking whether its contents changed since it was read from disk, and a pin count tracking how many operations are currently using it, since a pinned page can&#39;t be evicted, which is the same idea as the kernel&#39;s page reference count.&lt;/p&gt;
&lt;p&gt;When a page is requested the pool checks the page table, and if the page is already in a frame it pins it and returns the pointer, and if not it finds a victim frame through the eviction policy, writes the victim to disk first if it&#39;s dirty, reads the requested page from disk into that frame, updates the page table, and returns the pointer. It&#39;s the same flow as an OS page fault handler, just at a different layer.&lt;/p&gt;
&lt;h2 id=&quot;eviction-where-the-database-knows-more&quot;&gt;eviction, where the database knows more&lt;/h2&gt;
&lt;p&gt;The OS uses something like clock or a modified LRU that works across all processes, all files, and all pages, with no application-level knowledge, and a database can do better because it knows the access patterns. A sequential scan touches every page once, and a plain LRU would fill the cache with scan pages and evict hot index pages, which PostgreSQL avoids by using a small ring buffer for sequential scans so scan pages cycle through a handful of frames instead of polluting the whole pool. A B+tree lookup traverses root, then internal, then leaf, and the root page is hit on every lookup so it should basically never be evicted, which LRU handles naturally but a database can also just pin critical pages explicitly. Prefetching works better too, since the database knows it&#39;s doing a range scan on a B+tree and can issue async reads for the next few leaf pages before it needs them, which the OS page cache can&#39;t do because it only sees physical file offsets, not logical access patterns.&lt;/p&gt;
&lt;h2 id=&quot;dirty-pages-and-write-back&quot;&gt;dirty pages and write-back&lt;/h2&gt;
&lt;p&gt;This is where the difference matters most. The OS can flush a dirty page to disk whenever it wants, which is fine for normal files but dangerous for a database, because if a modified data page hits disk before the corresponding WAL record then crash recovery breaks, and the write-ahead logging rule is that the log goes first and the data page second. A buffer pool enforces this by checking, before it writes a dirty page back, that the WAL has been flushed up to the page&#39;s last modification LSN (Log Sequence Number), so the page doesn&#39;t go to disk until its log records are safe. This is impossible with mmap since the kernel has no concept of WAL ordering or LSNs and flushes when it feels like it.&lt;/p&gt;
&lt;h2 id=&quot;o-direct-and-bypassing-the-os-page-cache&quot;&gt;O_DIRECT and bypassing the OS page cache&lt;/h2&gt;
&lt;p&gt;Most serious databases open their files with &lt;code&gt;O_DIRECT&lt;/code&gt;, which tells the kernel to skip its own page cache entirely so reads and writes go straight between the database&#39;s buffer pool and the disk. Without it you&#39;d have the data in two places, once in the buffer pool and once in the OS page cache, which doubles the memory usage for no benefit since the database already manages caching and the OS cache is redundant. &lt;code&gt;O_DIRECT&lt;/code&gt; also gives the database precise control over I/O timing with no surprises from kernel write-back threads or memory pressure from the kernel evicting buffer pool pages.&lt;/p&gt;
&lt;p&gt;It isn&#39;t free to use though, since it requires buffers to be aligned to the filesystem block size, usually 512 bytes or 4KB, and the I/O sizes have to be aligned too, and if you get the alignment wrong the syscall fails with EINVAL, which is why most databases that use &lt;code&gt;O_DIRECT&lt;/code&gt; implement their own aligned allocation routines.&lt;/p&gt;
&lt;p&gt;PostgreSQL is an exception, since it uses the OS page cache through buffered I/O rather than &lt;code&gt;O_DIRECT&lt;/code&gt; and relies on &lt;code&gt;fsync&lt;/code&gt; to force data to disk, which simplifies some things but means PostgreSQL competes with the OS for memory management control.&lt;/p&gt;
&lt;h2 id=&quot;it-s-the-same-problem-at-a-different-layer&quot;&gt;it&#39;s the same problem at a different layer&lt;/h2&gt;
&lt;p&gt;The parallel is almost exact:&lt;/p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;OS Virtual Memory&lt;/th&gt;&lt;th&gt;Database Buffer Pool&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;Physical page frame&lt;/td&gt;&lt;td&gt;Buffer pool frame&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;Page table (virtual to physical)&lt;/td&gt;&lt;td&gt;Page table (page ID to frame)&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;Page fault handler&lt;/td&gt;&lt;td&gt;Buffer pool miss handler&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;Dirty bit in PTE&lt;/td&gt;&lt;td&gt;Dirty flag per frame&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;Reference count&lt;/td&gt;&lt;td&gt;Pin count&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;kswapd (page reclaim)&lt;/td&gt;&lt;td&gt;Eviction policy (LRU, clock)&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;Swap file&lt;/td&gt;&lt;td&gt;Data file on disk&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;write-back&lt;/code&gt; flush&lt;/td&gt;&lt;td&gt;WAL-ordered write-back&lt;/td&gt;&lt;/tr&gt;
&lt;/tbody&gt;&lt;/table&gt;
&lt;p&gt;The database takes this responsibility away from the OS because general-purpose policies don&#39;t work for database workloads, where eviction needs access-pattern awareness, write-back needs WAL ordering, and prefetching needs query-plan knowledge, and the OS has none of that context.&lt;/p&gt;
&lt;h2 id=&quot;notes&quot;&gt;notes&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;InnoDB (MySQL) uses a buffer pool with an LRU that splits into &quot;young&quot; and &quot;old&quot; sublists. New pages enter the old sublist, and only move to the young sublist if accessed again. This handles the scan-pollution problem.&lt;/li&gt;
&lt;li&gt;PostgreSQL&#39;s shared_buffers is its buffer pool. It uses a clock-sweep eviction policy.&lt;/li&gt;
&lt;li&gt;SQLite in WAL mode maintains its own page cache but sits on top of the OS page cache (no O_DIRECT). It works because SQLite targets small-to-medium databases where double-caching isn&#39;t expensive.&lt;/li&gt;
&lt;li&gt;The buffer pool is one of the first things a database student builds. It&#39;s simple in concept and brutal in the details (concurrency, latch ordering, I/O scheduling).&lt;/li&gt;
&lt;li&gt;Some databases are experimenting with letting the buffer pool manage allocation at finer granularity than pages. But pages have stuck around because they align with disk I/O boundaries.&lt;/li&gt;
&lt;/ul&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>Swap and Paging: What Actually Happens When Memory Fills Up</title>
        <published>2025-10-30T00:00:00+00:00</published>
        <updated>2025-10-30T00:00:00+00:00</updated>
        
        <author>
          <name>Unknown</name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://yazeed1s.github.io/posts/swap-paging/"/>
        <id>https://yazeed1s.github.io/posts/swap-paging/</id>
        
        <content type="html" xml:base="https://yazeed1s.github.io/posts/swap-paging/">&lt;p&gt;I kept hitting concepts like &quot;page fault&quot; and &quot;swap&quot; while reading memory disaggregation papers, so I figured I should actually understand what these mean at a low level before going further.&lt;/p&gt;
&lt;h2 id=&quot;what-swap-is&quot;&gt;what swap is&lt;/h2&gt;
&lt;p&gt;Swap is disk space that acts as overflow for RAM, so when physical memory fills up the kernel moves some data to swap, and later, if that data is needed again, it gets loaded back. That&#39;s basically it, but the messy part is in the details.&lt;/p&gt;
&lt;h2 id=&quot;pages&quot;&gt;pages&lt;/h2&gt;
&lt;p&gt;The kernel doesn&#39;t manage memory byte by byte because the bookkeeping would be too expensive, so instead it works in fixed-size chunks called &lt;strong&gt;pages&lt;/strong&gt;, usually 4KB.&lt;/p&gt;
&lt;p&gt;When you allocate memory you get pages, and when data moves to disk it moves as pages. The physical counterpart is called a &lt;strong&gt;frame&lt;/strong&gt;, same size, different name: pages are virtual, frames are physical.&lt;/p&gt;
&lt;p&gt;8GB of RAM gives you roughly 2 million frames, and a process might think it has way more pages than that, but most aren&#39;t backed by physical memory until they&#39;re actually used.&lt;/p&gt;
&lt;h2 id=&quot;page-tables-and-the-mmu&quot;&gt;page tables and the mmu&lt;/h2&gt;
&lt;p&gt;The CPU doesn&#39;t know about virtual addresses on its own, there&#39;s a &lt;strong&gt;Memory Management Unit (MMU)&lt;/strong&gt; that translates virtual addresses to physical ones using &lt;strong&gt;page tables&lt;/strong&gt;, which are data structures the kernel maintains that the MMU walks to find where a virtual page actually lives (which physical frame, or if it&#39;s not in RAM at all).&lt;/p&gt;
&lt;p&gt;Walking page tables on every memory access would be slow, so the MMU keeps a cache called the &lt;strong&gt;TLB (Translation Lookaside Buffer)&lt;/strong&gt; where recent translations are stored, and a hit is fast while a miss pays for the full page table walk. Most accesses hit the TLB, and that&#39;s what makes virtual memory practical.&lt;/p&gt;
&lt;h2 id=&quot;page-faults&quot;&gt;page faults&lt;/h2&gt;
&lt;p&gt;When a program accesses a virtual address, the MMU checks whether that page is in RAM, and if it&#39;s not, you get a &lt;strong&gt;page fault&lt;/strong&gt;. This isn&#39;t an error, it&#39;s just the kernel saying &quot;hold on, I need to go get that.&quot; The program accesses memory, the MMU finds the page isn&#39;t resident, the CPU traps to the kernel, the kernel figures out where the page lives (swap, file, or nowhere), loads it into a frame, updates the page table, and then the program resumes.&lt;/p&gt;
&lt;p&gt;There are two kinds. A minor fault is when the page is already somewhere in memory like the page cache or a shared mapping and the kernel just fixes the page table, which is fast. A major fault means the page has to be read from disk, which is really slow.&lt;/p&gt;
&lt;p&gt;Some faults are expected, so lazy allocation means the kernel doesn&#39;t back memory until you touch it and the first access causes a minor fault, copy-on-write means shared pages aren&#39;t copied until someone writes, and swapping means a page was evicted earlier and now needs to come back. Other faults mean bugs, like accessing a garbage address gives you SIGSEGV.&lt;/p&gt;
&lt;h2 id=&quot;paging-in-and-out&quot;&gt;paging in and out&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Page in&lt;/strong&gt; means loading a page from disk into RAM, and &lt;strong&gt;page out&lt;/strong&gt; means moving a page from RAM to disk to free space.&lt;/p&gt;
&lt;p&gt;When RAM is full and a new page is needed, the kernel picks a &lt;strong&gt;victim&lt;/strong&gt; (some page not accessed recently). If the victim is dirty (modified since it was loaded), the kernel writes it to swap first, and if it&#39;s clean, the kernel just drops it and reloads later if needed.&lt;/p&gt;
&lt;pre class=&quot;giallo z-l-code z-d-code&quot; &gt;&lt;code data-lang=&quot;plain&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;Before:&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;RAM:  [A][B][C][D] &amp;lt;- full&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;Swap: [empty]&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;Need page E. Pick B as victim.&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;After:&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;RAM:  [A][E][C][D]&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;Swap: [B]&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If B is accessed again, you take a major fault, load B, and evict something else; this happens constantly under memory pressure.&lt;/p&gt;
&lt;h2 id=&quot;the-dirty-bit&quot;&gt;the dirty bit&lt;/h2&gt;
&lt;p&gt;Each page has a &lt;strong&gt;dirty bit&lt;/strong&gt; that gets set if the page has been modified since it was loaded.&lt;/p&gt;
&lt;p&gt;It matters because clean pages can be dropped since the kernel can reload them from the file or wherever they came from, but dirty pages can&#39;t be dropped without writing them somewhere first. Anonymous memory like the heap and stack that&#39;s dirty goes to swap, and file-backed memory that&#39;s been modified goes back to the file, or swap, depends.&lt;/p&gt;
&lt;h2 id=&quot;why-disk-access-hurts&quot;&gt;why disk access hurts&lt;/h2&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Access&lt;/th&gt;&lt;th&gt;Latency&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;RAM&lt;/td&gt;&lt;td&gt;~100 nanoseconds&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;SSD&lt;/td&gt;&lt;td&gt;~100 microseconds&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;HDD&lt;/td&gt;&lt;td&gt;~10 milliseconds&lt;/td&gt;&lt;/tr&gt;
&lt;/tbody&gt;&lt;/table&gt;
&lt;p&gt;SSD is 1,000× slower than RAM, and HDD is 100,000× slower.&lt;/p&gt;
&lt;p&gt;A major fault means a disk access, which means the program stalls for an eternity in CPU time. One major fault doesn&#39;t matter, a hundred per second and the app feels sluggish, a thousand and the system is unusable.&lt;/p&gt;
&lt;h2 id=&quot;thrashing&quot;&gt;thrashing&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Thrashing&lt;/strong&gt; is what happens when the working set doesn&#39;t fit in RAM.&lt;/p&gt;
&lt;p&gt;Working set is the memory you&#39;re actively using right now, and if it&#39;s bigger than RAM, the kernel constantly swaps pages in and out because every page you load evicts something you need again soon.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Need page A -&amp;gt; fault, load A, evict B&lt;/li&gt;
&lt;li&gt;Need page B -&amp;gt; fault, load B, evict A&lt;/li&gt;
&lt;li&gt;Need page A -&amp;gt; fault, load A, evict B&lt;/li&gt;
&lt;li&gt;Forever&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The system spends 99% of its time moving data and 1% doing work, and this is where you get the &quot;stuck mouse&quot; feeling.&lt;/p&gt;
&lt;h2 id=&quot;lru-picking-victims&quot;&gt;lru, picking victims&lt;/h2&gt;
&lt;p&gt;How does the kernel decide which page to evict? The ideal strategy would be to evict the one that won&#39;t be needed soonest, but you can&#39;t predict the future, so the kernel approximates with &lt;strong&gt;LRU (Least Recently Used)&lt;/strong&gt; where pages not accessed in a while are good candidates.&lt;/p&gt;
&lt;p&gt;Linux maintains active/inactive lists where pages accessed recently go to active and cold pages drift to inactive, and reclaim takes from inactive first. True LRU would track exact access times for every page, which is too expensive, so Linux settles for &quot;recently used&quot; vs &quot;not recently used.&quot;&lt;/p&gt;
&lt;h2 id=&quot;swappiness&quot;&gt;swappiness&lt;/h2&gt;
&lt;p&gt;Linux doesn&#39;t wait until RAM is completely full to start swapping, there&#39;s a knob called &lt;strong&gt;swappiness&lt;/strong&gt;.&lt;/p&gt;
&lt;pre class=&quot;giallo z-l-code z-d-code&quot; &gt;&lt;code data-lang=&quot;shellscript&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt;$&lt;/span&gt;&lt;span class=&quot;z-l-3 z-d-6&quot;&gt; cat&lt;/span&gt;&lt;span class=&quot;z-l-3 z-d-6&quot;&gt; /proc/sys/vm/swappiness&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt;60&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Range is 0-200 and it affects how willing the kernel is to swap anonymous memory versus reclaim file cache. At 0 it avoids swapping and holds app memory while sacrificing cache, 60 is the default and balanced, and 100+ means swap more aggressively and keep the file cache warm. No universal right answer, depends on workload.&lt;/p&gt;
&lt;h2 id=&quot;what-can-t-be-swapped&quot;&gt;what can&#39;t be swapped&lt;/h2&gt;
&lt;p&gt;Not everything can go to disk. Kernel memory like page tables, process descriptors, and driver state must stay in RAM because if the kernel got paged out, who pages it back in? Pinned memory is memory explicitly locked by the application using mlock, and it&#39;s used by RDMA, databases, and similar systems. Kernel memory leaks are dangerous because that memory is gone until reboot.&lt;/p&gt;
&lt;h2 id=&quot;overcommit&quot;&gt;overcommit&lt;/h2&gt;
&lt;p&gt;Linux lets you allocate more memory than exists, so &lt;code&gt;malloc(1GB)&lt;/code&gt; succeeds even with 512MB free. This is intentional, called &lt;strong&gt;overcommit&lt;/strong&gt;, and it makes sense because most programs allocate more than they use (sparse arrays, forked processes before exec), and refusing would break software.&lt;/p&gt;
&lt;p&gt;The downside is that if you actually use all that memory the OOM killer fires, so the allocation succeeded but using it didn&#39;t.&lt;/p&gt;
&lt;pre class=&quot;giallo z-l-code z-d-code&quot; &gt;&lt;code data-lang=&quot;shellscript&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt;$&lt;/span&gt;&lt;span class=&quot;z-l-3 z-d-6&quot;&gt; cat&lt;/span&gt;&lt;span class=&quot;z-l-3 z-d-6&quot;&gt; /proc/sys/vm/overcommit_memory&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-2 z-d-3&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt;  #&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt; heuristic (default)&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt;#&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt; 0 = guess what&amp;#39;s safe&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt;#&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt; 1 = always allow (yolo)&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt;#&lt;/span&gt;&lt;span class=&quot;z-l-5 z-l-i z-d-1 z-d-i&quot;&gt; 2 = strict (refuse if exceeds limit)&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Mode 2 is safer but breaks things, and mode 1 is living dangerously.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Mode 0 isn&#39;t blind. The kernel uses heuristics that account for total RAM, swap space, and current usage. It will still refuse obviously absurd allocations. Mode 2 enforces a strict limit based on &lt;code&gt;overcommit_ratio&lt;/code&gt; (default 50%) of physical RAM plus swap. So &quot;always allow&quot; vs &quot;strict&quot; is more nuanced than it looks.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;why-this-matters-for-remote-memory&quot;&gt;why this matters for remote memory&lt;/h2&gt;
&lt;p&gt;The main problem is that disk is slow, 1,000-100,000× slower than RAM.&lt;/p&gt;
&lt;p&gt;Systems like Infiniswap replace swap with network access to remote memory, since RDMA gives single-digit microsecond latency, which is still slower than local RAM but 10-1000× faster than disk. Keep the paging model, replace the slow part, and the performance cliff becomes a slope.&lt;/p&gt;
&lt;p&gt;The interesting thing is that Linux has a &lt;strong&gt;frontswap&lt;/strong&gt; interface, which is a hook that lets you intercept pages before they go to disk, so if you implement a few callbacks your module becomes an alternative swap backend, and that&#39;s how Infiniswap plugs into the kernel, where pages that would go to disk get redirected over the network instead.&lt;/p&gt;
&lt;p&gt;I want to look at this interface in more detail later, how frontswap works, what the callbacks look like, and what&#39;s involved in building something like Infiniswap. Different post.&lt;/p&gt;
&lt;h2 id=&quot;notes&quot;&gt;notes&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Page size usually 4KB. Huge pages exist (2MB, 1GB) to reduce TLB pressure.&lt;/li&gt;
&lt;li&gt;&quot;Anonymous memory&quot; = heap, stack (no backing file). &quot;File-backed&quot; = mmap&#39;d files, page cache.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;vmstat&lt;/code&gt;, &lt;code&gt;sar&lt;/code&gt;, &lt;code&gt;/proc/meminfo&lt;/code&gt; for monitoring paging activity.&lt;/li&gt;
&lt;li&gt;Swap on SSD helps. Swap on HDD is pain.&lt;/li&gt;
&lt;li&gt;zswap = compressed swap cache in RAM. Buys time before hitting disk.&lt;/li&gt;
&lt;/ul&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>What a Process Owns</title>
        <published>2025-10-19T00:00:00+00:00</published>
        <updated>2025-10-19T00:00:00+00:00</updated>
        
        <author>
          <name>Unknown</name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://yazeed1s.github.io/posts/what-a-process-owns/"/>
        <id>https://yazeed1s.github.io/posts/what-a-process-owns/</id>
        
        <content type="html" xml:base="https://yazeed1s.github.io/posts/what-a-process-owns/">&lt;p&gt;A process isn&#39;t just &quot;running code.&quot; It&#39;s a container that owns a set of resources the kernel manages on its behalf, and understanding what those resources are helps explain why certain operations behave the way they do.&lt;/p&gt;
&lt;h2 id=&quot;address-space&quot;&gt;address space&lt;/h2&gt;
&lt;p&gt;Each process gets its own virtual address space, which is a complete illusion of having memory all to itself. On 64-bit Linux a process gets a 128TB virtual space (48-bit addressing), split into user space (lower half) and kernel space (upper half, shared across all processes but inaccessible from user mode).&lt;/p&gt;
&lt;p&gt;Inside this space you have the text segment (your compiled code, read-only and executable), the data segment for initialized globals, BSS for uninitialized globals (zeroed by the kernel), the heap growing upward from the end of BSS (managed by malloc/brk/mmap), the stack growing downward from near the top of user space, and memory-mapped regions scattered in between for shared libraries, mmap&#39;d files, and anonymous mappings.&lt;/p&gt;
&lt;p&gt;Two processes can have the same virtual address pointing to completely different physical memory because each process has its own page table. The kernel switches page tables on every context switch by writing a new value to CR3 on x86.&lt;/p&gt;
&lt;p&gt;You can look at a process&#39;s memory map through &lt;code&gt;/proc/&amp;lt;pid&amp;gt;/maps&lt;/code&gt;, which shows each VMA (Virtual Memory Area) with its address range, permissions, offset, device, inode, and pathname.&lt;/p&gt;
&lt;h2 id=&quot;file-descriptors&quot;&gt;file descriptors&lt;/h2&gt;
&lt;p&gt;A file descriptor is an integer index into a per-process table of open files. When you call &lt;code&gt;open()&lt;/code&gt;, the kernel creates an entry pointing to the underlying file object and returns the lowest available integer, which is why &lt;code&gt;stdin&lt;/code&gt; is 0, &lt;code&gt;stdout&lt;/code&gt; is 1, and &lt;code&gt;stderr&lt;/code&gt; is 2 (they&#39;re opened first).&lt;/p&gt;
&lt;p&gt;Each process has its own file descriptor table, so fd 5 in one process is completely unrelated to fd 5 in another. The file descriptor carries the current read/write position, flags (like &lt;code&gt;O_NONBLOCK&lt;/code&gt;), and a reference to the underlying inode.&lt;/p&gt;
&lt;p&gt;File descriptors aren&#39;t just for files on disk, they also represent pipes, sockets, eventfd, timerfd, signalfd, epoll instances, and even directories, and the &quot;everything is a file&quot; philosophy means one interface for many types of I/O.&lt;/p&gt;
&lt;p&gt;There&#39;s a per-process limit on open file descriptors (check with &lt;code&gt;ulimit -n&lt;/code&gt;, default often 1024) and a system-wide limit. Leaking file descriptors is a common bug, especially in long-running services that open connections and forget to close them.&lt;/p&gt;
&lt;p&gt;You can see all open file descriptors for a process in &lt;code&gt;/proc/&amp;lt;pid&amp;gt;/fd/&lt;/code&gt;, where each entry is a symlink to the actual file, socket, or pipe.&lt;/p&gt;
&lt;h2 id=&quot;signals&quot;&gt;signals&lt;/h2&gt;
&lt;p&gt;Each process has a signal disposition table describing what happens for each signal, and the default action depends on the signal, so &lt;code&gt;SIGTERM&lt;/code&gt; terminates, &lt;code&gt;SIGSTOP&lt;/code&gt; suspends, and &lt;code&gt;SIGCHLD&lt;/code&gt; is ignored by default. You can override most defaults with signal handlers, except for &lt;code&gt;SIGKILL&lt;/code&gt; and &lt;code&gt;SIGSTOP&lt;/code&gt; which can&#39;t be caught or ignored.&lt;/p&gt;
&lt;p&gt;A process also has a signal mask that blocks specific signals from being delivered (they queue up until unblocked), and a set of pending signals that have been sent but not yet handled.&lt;/p&gt;
&lt;p&gt;Signal handling is per-process (signal disposition) but delivery can target specific threads. &lt;code&gt;kill(pid, sig)&lt;/code&gt; sends to the process, &lt;code&gt;pthread_kill(tid, sig)&lt;/code&gt; targets a thread.&lt;/p&gt;
&lt;h2 id=&quot;credentials&quot;&gt;credentials&lt;/h2&gt;
&lt;p&gt;Every process has a set of UIDs and GIDs that determine what it can access, and there are three sets, the real one being who actually started the process, the effective one being what&#39;s checked for access, and the saved one being the previous effective so you can drop and regain privileges.&lt;/p&gt;
&lt;p&gt;Usually real and effective are the same, but setuid programs differ. When you run &lt;code&gt;passwd&lt;/code&gt;, the file has the setuid bit set, so the effective UID becomes root (owner of the file) while the real UID stays yours. The process can access &lt;code&gt;/etc/shadow&lt;/code&gt; because effective UID is root, but the program knows who actually called it through the real UID.&lt;/p&gt;
&lt;p&gt;Groups work the same way, and there&#39;s also a supplementary group list for all the groups you belong to. All of this is stored in the process&#39;s credential structure in the kernel.&lt;/p&gt;
&lt;h2 id=&quot;scheduling-context&quot;&gt;scheduling context&lt;/h2&gt;
&lt;p&gt;The kernel tracks scheduling metadata for each process, so its priority (nice value from -20 to 19, and real-time priority if applicable), which scheduling class it belongs to (CFS for normal processes, FIFO or round-robin for real-time), how much CPU time it has consumed, when it last ran, and which CPU it ran on.&lt;/p&gt;
&lt;p&gt;CFS (Completely Fair Scheduler) on Linux uses a virtual runtime to track how much CPU time a process has received relative to others, and it always picks the process with the lowest virtual runtime to run next. The nice value adjusts the weight, so a lower nice value means more weight, which means the virtual runtime advances more slowly, which means more CPU time.&lt;/p&gt;
&lt;h2 id=&quot;namespaces-and-cgroups&quot;&gt;namespaces and cgroups&lt;/h2&gt;
&lt;p&gt;In containerized environments processes also own namespace memberships and cgroup associations. Namespaces give a process an isolated view of system resources, so the PID namespace makes it think its PID is 1, the network namespace gives it a separate network stack, and the mount namespace gives it a different filesystem view. Cgroups limit how much resource a process or group of processes can use, so CPU quota, memory limit, I/O bandwidth, and number of PIDs, and these are the building blocks of containers.&lt;/p&gt;
&lt;h2 id=&quot;the-task-struct&quot;&gt;the task_struct&lt;/h2&gt;
&lt;p&gt;Internally the kernel represents all of this in a single structure called &lt;code&gt;task_struct&lt;/code&gt;, which on Linux is a large struct of several kilobytes containing or pointing to everything described above, so the memory descriptor for address space, the files struct for file descriptors, the signal handler table, credentials, scheduling state, namespace pointers, cgroup references, and many other fields.&lt;/p&gt;
&lt;p&gt;Every running thread has a &lt;code&gt;task_struct&lt;/code&gt;, so a single-threaded process has one and a multi-threaded process has one per thread, and they share the memory descriptor, file table, and signal handlers through pointers to the same structures.&lt;/p&gt;
&lt;h2 id=&quot;notes&quot;&gt;notes&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;/proc/&amp;lt;pid&amp;gt;/status&lt;/code&gt; gives a summary of most of these: UIDs, GIDs, threads, memory usage, signal masks, context switch counts&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/proc/&amp;lt;pid&amp;gt;/maps&lt;/code&gt; for address space layout&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/proc/&amp;lt;pid&amp;gt;/fd/&lt;/code&gt; for open file descriptors&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/proc/&amp;lt;pid&amp;gt;/cgroup&lt;/code&gt; for cgroup membership&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ls -la /proc/&amp;lt;pid&amp;gt;/ns/&lt;/code&gt; shows namespace memberships&lt;/li&gt;
&lt;li&gt;&lt;code&gt;strace -p &amp;lt;pid&amp;gt;&lt;/code&gt; lets you watch syscalls in real time&lt;/li&gt;
&lt;li&gt;A zombie process has exited but its parent hasn&#39;t called &lt;code&gt;wait()&lt;/code&gt;, so the task_struct stays around holding the exit status&lt;/li&gt;
&lt;/ul&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>Interrupts, Traps, and the Kernel Boundary</title>
        <published>2025-02-18T00:00:00+00:00</published>
        <updated>2025-02-18T00:00:00+00:00</updated>
        
        <author>
          <name>Unknown</name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://yazeed1s.github.io/posts/interrupts-traps/"/>
        <id>https://yazeed1s.github.io/posts/interrupts-traps/</id>
        
        <content type="html" xml:base="https://yazeed1s.github.io/posts/interrupts-traps/">&lt;p&gt;Your app might call a syscall, a packet might arrive on the network card, or the timer might fire; all of these interrupt normal execution, but they&#39;re not the same thing. The terminology gets confusing because people use interrupt, trap, and exception loosely.&lt;/p&gt;
&lt;h2 id=&quot;user-space-vs-kernel-space&quot;&gt;user space vs kernel space&lt;/h2&gt;
&lt;p&gt;Starting from the basics, modern CPUs have privilege levels called rings on x86, where ring 0 is the most privileged (kernel) and ring 3 is the least privileged (user applications).&lt;/p&gt;
&lt;p&gt;Your application runs in ring 3 and can execute normal instructions, access its own memory, and do math, but it cannot access hardware directly, read/write arbitrary memory addresses, or execute privileged instructions (like changing page tables or disabling interrupts). The kernel runs in ring 0 and can do all of those things.&lt;/p&gt;
&lt;p&gt;When you call &lt;code&gt;read()&lt;/code&gt; to read from a file, your code can&#39;t just talk to the disk controller, it has to ask the kernel. The CPU has to switch from ring 3 to ring 0, do the privileged work, then switch back.&lt;/p&gt;
&lt;p&gt;This switch is the kernel boundary, and crossing it costs you a register save and restore, a privilege change, and sometimes cache disruption, which is why syscalls aren&#39;t free.&lt;/p&gt;
&lt;h2 id=&quot;interrupts&quot;&gt;interrupts&lt;/h2&gt;
&lt;p&gt;An interrupt is a signal from hardware that says &quot;stop what you&#39;re doing, I need attention.&quot; Examples are a keyboard telling you a key was pressed, a network card saying a packet arrived, a timer saying your time slice is up, or a disk controller saying that read you asked for is done.&lt;/p&gt;
&lt;p&gt;Interrupts are asynchronous, they happen whenever the hardware needs attention regardless of what the CPU is currently doing. You could be in the middle of a for loop and suddenly an interrupt fires.&lt;/p&gt;
&lt;p&gt;When an interrupt happens, the CPU stops executing the current instruction stream, saves the current state (registers, instruction pointer, flags), looks up the interrupt handler in the Interrupt Descriptor Table (IDT), jumps to that handler (now in ring 0), the handler does its work, and then the handler returns and the CPU restores state and continues where it left off. The key thing is that the currently running process doesn&#39;t trigger this, it just happens to it.&lt;/p&gt;
&lt;h2 id=&quot;traps&quot;&gt;traps&lt;/h2&gt;
&lt;p&gt;A trap is a synchronous exception triggered by the currently running code, and it&#39;s intentional, the main example being syscalls, where when you call &lt;code&gt;read()&lt;/code&gt; the C library eventually executes a special instruction (&lt;code&gt;syscall&lt;/code&gt; on x86-64, &lt;code&gt;int 0x80&lt;/code&gt; on older x86) that deliberately triggers a trap.&lt;/p&gt;
&lt;pre class=&quot;giallo z-l-code z-d-code&quot; &gt;&lt;code data-lang=&quot;plain&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;User code calls read()&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;  -&amp;gt; libc wrapper&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;    -&amp;gt; syscall instruction (trap into kernel)&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;      -&amp;gt; kernel syscall handler&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;        -&amp;gt; returns to user space&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The difference from interrupts is that you asked for this, the code executing triggered it, and it happens at a specific point in your instruction stream, not randomly.&lt;/p&gt;
&lt;p&gt;Other traps and exceptions include page faults (you accessed memory that isn&#39;t mapped, which could be a bug or could be demand paging doing its job), division by zero (arithmetic error), invalid opcode (tried to execute garbage), and breakpoint traps (int 3 for debuggers). Some of these are errors that kill your process (division by zero), and some are handled so execution continues (page fault loads the page and then your load instruction retries).&lt;/p&gt;
&lt;h2 id=&quot;the-naming-confusion&quot;&gt;the naming confusion&lt;/h2&gt;
&lt;p&gt;Different sources use these terms differently, and the way I think about it is that an interrupt is external, async, and from hardware, a trap is internal, sync, and intentional like a syscall, an exception is internal, sync, and usually an error like a page fault or div by zero, a fault is an exception that can be corrected where the instruction retries, and an abort is an unrecoverable error.&lt;/p&gt;
&lt;p&gt;Some people use &quot;exception&quot; as the umbrella term for everything, some use &quot;interrupt&quot; for everything, and the Intel manual has its own definitions. It&#39;s messy. What matters is understanding whether the trigger is external (hardware) or internal (executing code), and whether it&#39;s expected (syscall) or unexpected (error).&lt;/p&gt;
&lt;h2 id=&quot;the-interrupt-descriptor-table&quot;&gt;the interrupt descriptor table&lt;/h2&gt;
&lt;p&gt;The CPU needs to know where to jump for each interrupt or exception, and this is stored in the IDT, a table in memory that the kernel sets up at boot. Each entry has a handler address, what privilege level can trigger it, and a gate type (interrupt gate, trap gate).&lt;/p&gt;
&lt;p&gt;For hardware interrupts, the entries point to kernel interrupt handlers, and for the syscall trap, it points to the syscall entry point. When an interrupt fires, the CPU uses the interrupt number as an index into the IDT, checks privilege, switches to ring 0 if needed, and jumps to the handler address.&lt;/p&gt;
&lt;h2 id=&quot;hardware-interrupts-in-more-detail&quot;&gt;hardware interrupts in more detail&lt;/h2&gt;
&lt;p&gt;When a device needs attention, it signals an interrupt request (IRQ), and on modern systems this goes through an interrupt controller (APIC). The kernel has to acknowledge the interrupt, figure out which device caused it, call the right driver&#39;s handler, and tell the interrupt controller we&#39;re done. Handling needs to be fast because interrupts are disabled (or that IRQ is masked) while you&#39;re in the handler, and if you take too long you miss other interrupts.&lt;/p&gt;
&lt;p&gt;Linux splits this into a top half and a bottom half, where the top half runs in interrupt context, does the minimum work, and schedules the bottom half, and the bottom half runs later with interrupts enabled and does the real work through softirqs, tasklets, and workqueues. A network card interrupt&#39;s top half grabs the packet from hardware, queues it, and schedules the bottom half, which then processes the packet up the network stack.&lt;/p&gt;
&lt;h2 id=&quot;syscall-cost&quot;&gt;syscall cost&lt;/h2&gt;
&lt;p&gt;Crossing the kernel boundary isn&#39;t free, since you pay for saving and restoring registers, switching from the user stack to the kernel stack, TLB and cache effects, and Spectre mitigations on modern kernels like KPTI and retpolines. On a modern system a syscall might take a few hundred nanoseconds, which doesn&#39;t sound like much, but if you&#39;re doing thousands per second it adds up, and that cost is why people use batching to do more work per call, io_uring to submit many I/O requests with one syscall, and mmap to access files without read() syscalls at all.&lt;/p&gt;
&lt;h2 id=&quot;notes&quot;&gt;notes&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;On x86-64, &lt;code&gt;syscall&lt;/code&gt;/&lt;code&gt;sysret&lt;/code&gt; are faster than the old &lt;code&gt;int 0x80&lt;/code&gt; method&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/proc/interrupts&lt;/code&gt; shows interrupt counts per CPU&lt;/li&gt;
&lt;li&gt;&lt;code&gt;perf stat&lt;/code&gt; can count context switches and syscalls&lt;/li&gt;
&lt;li&gt;NMI (Non-Maskable Interrupt) can&#39;t be disabled, used for profiling and panic&lt;/li&gt;
&lt;li&gt;The timer interrupt is what makes preemptive multitasking work&lt;/li&gt;
&lt;/ul&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>Context Switches: What Actually Happens</title>
        <published>2024-11-18T00:00:00+00:00</published>
        <updated>2024-11-18T00:00:00+00:00</updated>
        
        <author>
          <name>Unknown</name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://yazeed1s.github.io/posts/context-switches/"/>
        <id>https://yazeed1s.github.io/posts/context-switches/</id>
        
        <content type="html" xml:base="https://yazeed1s.github.io/posts/context-switches/">&lt;p&gt;A context switch is what happens when the OS takes one process off the CPU and puts another one on. It happens constantly, thousands of times per second, and your programs never notice because the whole point is that it looks seamless.&lt;/p&gt;
&lt;h2 id=&quot;why-context-switches-exist&quot;&gt;why context switches exist&lt;/h2&gt;
&lt;p&gt;CPUs don&#39;t multitask on their own, they execute one instruction stream at a time (per core). The OS creates the illusion of parallelism by rapidly switching between processes, giving each one a time slice of maybe 1-10 milliseconds, executing its instructions, then saving its state and loading the next process&#39;s state. Do this fast enough and it looks like everything runs at the same time.&lt;/p&gt;
&lt;h2 id=&quot;what-gets-saved&quot;&gt;what gets saved&lt;/h2&gt;
&lt;p&gt;A process&#39;s execution state lives in CPU registers, and when the OS switches away from a process it needs to save all of them, so the general purpose registers (rax, rbx, rcx on x86-64), the instruction pointer (rip, which says where execution was), the stack pointer (rsp), the flags register, the floating point and SIMD registers (SSE, AVX), and the memory mappings reference (CR3 on x86, which points to the page tables).&lt;/p&gt;
&lt;p&gt;All of this gets saved to the process&#39;s kernel data structure (the task_struct on Linux), and the incoming process&#39;s saved state gets loaded into the CPU registers. The CPU then continues executing from wherever the new process left off.&lt;/p&gt;
&lt;h2 id=&quot;what-actually-happens-step-by-step&quot;&gt;what actually happens step by step&lt;/h2&gt;
&lt;p&gt;The timer interrupt fires (or the process yields, or it blocks on I/O), the CPU traps to the kernel, the scheduler picks the next process, the kernel saves current registers to the outgoing task_struct, loads registers from the incoming task_struct, switches the page tables by writing CR3 (which changes the entire virtual memory mapping), flushes TLB entries that are no longer valid, and returns to user space where the new process resumes as if nothing happened.&lt;/p&gt;
&lt;pre class=&quot;giallo z-l-code z-d-code&quot; &gt;&lt;code data-lang=&quot;plain&quot;&gt;&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;Process A running&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;  -&amp;gt; timer interrupt fires&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;  -&amp;gt; save A&amp;#39;s registers to A&amp;#39;s task_struct&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;  -&amp;gt; scheduler picks Process B&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;  -&amp;gt; load B&amp;#39;s registers from B&amp;#39;s task_struct&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;  -&amp;gt; switch page tables (CR3)&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;  -&amp;gt; flush TLB&lt;/span&gt;&lt;/span&gt;
&lt;span class=&quot;giallo-l&quot;&gt;&lt;span&gt;  -&amp;gt; Process B is now running&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The key thing is that the process doesn&#39;t know. It saved no state, it called no function. The kernel did everything while the process wasn&#39;t looking.&lt;/p&gt;
&lt;h2 id=&quot;the-cost&quot;&gt;the cost&lt;/h2&gt;
&lt;p&gt;Context switches aren&#39;t free. The direct cost is saving and restoring register state, which is maybe a few hundred nanoseconds, but the indirect cost is worse, because the TLB gets flushed partially or fully since the new process has different page tables, so the first memory accesses after the switch take page table walks instead of TLB hits, and the CPU caches are now full of the old process&#39;s data so the new process suffers cache misses until it warms them up, and the branch predictors trained on the old process&#39;s code are useless for the new one. These indirect costs can add up to several microseconds of effective penalty, and on workloads with many short-lived operations it can matter a lot.&lt;/p&gt;
&lt;h2 id=&quot;thread-switches-vs-process-switches&quot;&gt;thread switches vs process switches&lt;/h2&gt;
&lt;p&gt;Threads within the same process share address space, so switching between them doesn&#39;t require changing CR3 or flushing the TLB, which makes thread switches cheaper, since you still save and restore registers but skip the expensive page table swap and TLB invalidation. This is one reason multi-threaded servers outperform multi-process ones for high-concurrency workloads, the context switches are fewer and cheaper.&lt;/p&gt;
&lt;h2 id=&quot;voluntary-vs-involuntary&quot;&gt;voluntary vs involuntary&lt;/h2&gt;
&lt;p&gt;A &lt;strong&gt;voluntary&lt;/strong&gt; context switch happens when a process can&#39;t continue, so it calls &lt;code&gt;read()&lt;/code&gt; and waits for disk, calls &lt;code&gt;sleep()&lt;/code&gt;, waits on a mutex, or does any blocking operation, and it&#39;s basically the process saying it has nothing to do so give the CPU to someone else. An &lt;strong&gt;involuntary&lt;/strong&gt; context switch happens when the scheduler preempts the process because its time slice expired and the timer interrupt fired and the scheduler decided it&#39;s someone else&#39;s turn, or a higher-priority process became runnable, or load balancing moved the process to another core. You can see both types in &lt;code&gt;/proc/&amp;lt;pid&amp;gt;/status&lt;/code&gt; under &lt;code&gt;voluntary_ctxt_switches&lt;/code&gt; and &lt;code&gt;nonvoluntary_ctxt_switches&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id=&quot;what-triggers-a-switch&quot;&gt;what triggers a switch&lt;/h2&gt;
&lt;p&gt;Most context switches come from I/O waits (process blocks, voluntary), timer expiry (time slice used up, involuntary), synchronization (mutex, semaphore, condition variable), and inter-process communication (pipe, signal). A busy process that never blocks still gets switched out involuntarily when its time slice expires.&lt;/p&gt;
&lt;h2 id=&quot;notes&quot;&gt;notes&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;perf stat&lt;/code&gt; shows context switch counts for a command&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/proc/&amp;lt;pid&amp;gt;/status&lt;/code&gt; shows voluntary and involuntary counts per process&lt;/li&gt;
&lt;li&gt;&lt;code&gt;vmstat&lt;/code&gt; shows system-wide context switches per second (cs column)&lt;/li&gt;
&lt;li&gt;On a typical desktop system you might see 10,000-50,000 context switches per second&lt;/li&gt;
&lt;li&gt;PCID (Process Context ID) on modern x86 lets the TLB keep entries from multiple processes, reducing the flush cost&lt;/li&gt;
&lt;li&gt;Kernel preemption means the kernel itself can be context-switched mid-operation (when configured with &lt;code&gt;PREEMPT&lt;/code&gt;)&lt;/li&gt;
&lt;/ul&gt;
</content>
        
    </entry>
</feed>
