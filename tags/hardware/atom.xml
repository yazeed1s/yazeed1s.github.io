<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <title>Yazeed&#39;s Blog - hardware</title>
    <subtitle>Notes on systems and low-level software.</subtitle>
    <link rel="self" type="application/atom+xml" href="https://yazeed1s.github.io/tags/hardware/atom.xml"/>
    <link rel="alternate" type="text/html" href="https://yazeed1s.github.io"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>2026-02-12T00:00:00+00:00</updated>
    <id>https://yazeed1s.github.io/tags/hardware/atom.xml</id>
    <entry xml:lang="en">
        <title>Tiered Memory</title>
        <published>2026-02-12T00:00:00+00:00</published>
        <updated>2026-02-12T00:00:00+00:00</updated>
        
        <author>
          <name>Unknown</name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://yazeed1s.github.io/posts/memory-teiring/"/>
        <id>https://yazeed1s.github.io/posts/memory-teiring/</id>
        
        <content type="html" xml:base="https://yazeed1s.github.io/posts/memory-teiring/">&lt;p&gt;The OS always assumed memory is uniform, where every page frame is the same speed, same cost, same latency, and with CXL and tiered memory that assumption breaks because you now have fast DRAM and slower memory in the same machine. At first it sounded simple to me, hot pages go in fast memory and cold pages in slow memory, but from an OS perspective it gets complicated fast.&lt;/p&gt;
&lt;h2 id=&quot;first-what-are-memory-pages&quot;&gt;first, what are memory pages&lt;/h2&gt;
&lt;p&gt;Before talking about tiers it&#39;s worth being clear on pages. Operating systems manage memory in fixed-size chunks called pages, which on x86 is 4KB by default, though sometimes you use huge pages of 2MB or 1GB. The OS doesn&#39;t think in bytes, it thinks in pages. When a process allocates memory the OS maps virtual pages to physical page frames and the page table stores this mapping, so the CPU sees a virtual address, walks the page tables, and translates it to a physical frame. If a page isn&#39;t present you get a page fault, and if memory is full the OS evicts pages. So when you talk about tiered memory what you&#39;re really asking is which physical page frames should live in which kind of memory, and that&#39;s the core question.&lt;/p&gt;
&lt;h2 id=&quot;why-do-we-even-need-tiered-memory&quot;&gt;why do we even need tiered memory&lt;/h2&gt;
&lt;p&gt;A server has DRAM directly attached to the CPU through memory channels, which is fast and low latency but expensive, and in large systems memory becomes a serious cost factor, in some cloud setups a big portion of total server cost, and many applications allocate large heaps but only actively touch part of them. Scaling DRAM isn&#39;t trivial either since you&#39;re limited by channels, DIMM slots, and signal integrity. So the idea behind tiered memory is that instead of making all memory equally fast and equally expensive you have a small fast tier and a larger slower tier, put frequently used pages in fast memory and less active pages in slower memory. Conceptually simple, the implementation is not.&lt;/p&gt;
&lt;h2 id=&quot;cxl-and-heterogeneous-memory&quot;&gt;CXL and heterogeneous memory&lt;/h2&gt;
&lt;p&gt;With newer interconnects you can attach extra memory that&#39;s cache-coherent but slower than local DRAM, and from the OS perspective it looks like another NUMA node. The latency is higher though, roughly:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Local DRAM: maybe around 100ns&lt;/li&gt;
&lt;li&gt;Attached memory over fabric: maybe 2x or 3x that&lt;/li&gt;
&lt;li&gt;Still much faster than SSD or disk&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;So now you have heterogeneous memory inside the same system, where the fast tier is local DRAM and the slow tier is attached or remote memory, same abstraction of a page, different performance.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;The &quot;2x or 3x&quot; latency for CXL-attached memory is a rough estimate based on early CXL 1.1/2.0 hardware. Actual latency depends on the CXL device type (Type 1, 2, or 3), the number of CXL hops, the controller implementation, and whether the access hits the device&#39;s internal cache. Some CXL memory expanders report closer to 1.5x for cached accesses. These numbers will keep changing as the hardware matures.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;This breaks the old assumption that memory is uniform.&lt;/p&gt;
&lt;h2 id=&quot;the-core-problem-which-pages-go-where&quot;&gt;the core problem, which pages go where&lt;/h2&gt;
&lt;p&gt;If hot pages sit in the fast tier everything is fine, and if hot pages end up in the slow tier performance drops, so you need to detect which pages are hot and move them. The naive idea is to count how many times each page is accessed and treat the most accessed pages as hot, but it turns out that&#39;s not enough.&lt;/p&gt;
&lt;h2 id=&quot;hotness-is-not-that-simple&quot;&gt;hotness is not that simple&lt;/h2&gt;
&lt;p&gt;Frequency alone doesn&#39;t always tell the full story, because modern CPUs overlap memory accesses, so if several cache misses happen at the same time the effective stall per access can be smaller, and some accesses hurt more than others depending on timing and overlap. So a page can be frequently accessed but not necessarily performance-critical, and another page might be accessed less often but sit directly on the critical path. Instead of just asking how many times a page was accessed, you probably need to ask how much it slows down the program if it&#39;s in slow memory, which is a harder question, and I&#39;m not sure how well current systems actually answer it since it connects OS policy with microarchitecture behavior and I don&#39;t think the abstractions we have right now are set up for that.&lt;/p&gt;
&lt;h2 id=&quot;page-granularity-mismatch&quot;&gt;page granularity mismatch&lt;/h2&gt;
&lt;p&gt;The OS moves memory in 4KB pages but the hardware accesses memory in 64-byte cache lines, and sometimes only a few cache lines inside a 4KB page are really hot while the rest is barely touched, so if you migrate the entire page to the fast tier because a small region inside it is hot you&#39;re wasting precious fast memory. Huge pages make this worse since a 2MB page may contain a small hot region and a lot of cold data, and promoting the whole thing seems expensive. There&#39;s a mismatch between the OS abstraction, which is page-based, and real access behavior, which is cache-line based, and tiered memory just makes the mismatch more visible.&lt;/p&gt;
&lt;h2 id=&quot;migration-is-not-free&quot;&gt;migration is not free&lt;/h2&gt;
&lt;p&gt;Moving a page between tiers isn&#39;t just a pointer update, since you need to allocate space in the target tier, copy 4KB of data, update page tables, possibly flush TLB entries, and coordinate across cores. If you migrate too often, or migrate the wrong pages, you can hurt performance instead of improving it, and I&#39;ve seen papers where the migration overhead alone ate most of the benefit. So it becomes a control problem where you need accurate detection, low tracking overhead, stable decisions, and limited oscillation, and it starts to feel like scheduling honestly, continuously adapting to workload behavior except the feedback signals are noisier.&lt;/p&gt;
&lt;h2 id=&quot;where-disaggregation-fits&quot;&gt;where disaggregation fits&lt;/h2&gt;
&lt;p&gt;Tiered memory is closely related to disaggregated memory, since disaggregation means memory can be separated from compute and accessed over a fabric, and that memory naturally has higher latency than local DRAM so it often becomes the slow tier. At that point memory management isn&#39;t just a local kernel concern, it interacts with cluster design, resource allocation, and even scheduling across machines, and the boundary between OS and infrastructure gets thinner.&lt;/p&gt;
&lt;h2 id=&quot;notes-random-thoughts&quot;&gt;notes / random thoughts&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Page abstraction worked well when memory was uniform. Now it feels slightly strained.&lt;/li&gt;
&lt;li&gt;Counting accesses is easy. Understanding performance impact is harder. I&#39;m not convinced anyone has a great solution for this yet.&lt;/li&gt;
&lt;li&gt;Huge pages help TLB reach but can complicate tiering decisions.&lt;/li&gt;
&lt;li&gt;Migration policy starts to look like a feedback controller.&lt;/li&gt;
&lt;li&gt;There&#39;s always tension between transparency and giving applications more control.&lt;/li&gt;
&lt;li&gt;In some sense tiered memory is like swap inside RAM, but at nanosecond scale.&lt;/li&gt;
&lt;li&gt;It looks like a small hardware change but from the OS side it touches a lot of assumptions.&lt;/li&gt;
&lt;/ul&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>CXL: Why Datacenter Memory is Getting a New Tier</title>
        <published>2026-01-27T00:00:00+00:00</published>
        <updated>2026-01-27T00:00:00+00:00</updated>
        
        <author>
          <name>Unknown</name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://yazeed1s.github.io/posts/cxl/"/>
        <id>https://yazeed1s.github.io/posts/cxl/</id>
        
        <content type="html" xml:base="https://yazeed1s.github.io/posts/cxl/">&lt;p&gt;DRAM can be half of server cost, and a lot of it still sits idle. One machine thrashes while the one next to it uses 30% of its memory. CXL is trying to fix that mismatch.&lt;/p&gt;
&lt;h2 id=&quot;the-problem&quot;&gt;the problem&lt;/h2&gt;
&lt;p&gt;Memory in datacenters is expensive and wasted at the same time. DRAM can be 50% of server cost, and research keeps showing that utilization is terrible. One machine is thrashing because it ran out of memory while another machine next to it is sitting at 30% usage. Some papers claim 70% of aggregate memory is underutilized across a cluster.&lt;/p&gt;
&lt;p&gt;The obvious thought is to share memory across machines the way we do with storage, pool it, so if one server has idle memory another server could use it. The issue is that storage can tolerate milliseconds of latency and memory can&#39;t, since a cache miss to local DRAM is around 100ns and going over the network with RDMA puts you at 1-5 microseconds, which is 10-50x slower, and for memory access patterns that&#39;s a lot. CXL is supposed to bridge this gap.&lt;/p&gt;
&lt;h2 id=&quot;what-it-actually-is&quot;&gt;what it actually is&lt;/h2&gt;
&lt;p&gt;CXL stands for Compute Express Link. It runs on the PCIe physical layer, same cables and slots, but with a different protocol on top.&lt;/p&gt;
&lt;p&gt;What matters is that it&#39;s cache-coherent. The CPU can do normal load/store to CXL-attached memory through the same memory path, without special APIs, RDMA-style verbs, or memory registration. The memory controller treats it like another memory region, basically another NUMA node.&lt;/p&gt;
&lt;p&gt;There are three protocols in the spec. CXL.io is basically just PCIe, for device discovery and config, boring stuff. CXL.cache lets devices cache host memory, useful for accelerators. CXL.mem is the interesting one, it lets the host access device-attached memory with load/store.&lt;/p&gt;
&lt;p&gt;CXL 1.0 and 1.1 are mostly local expansion. You plug a CXL card with DRAM into a PCIe slot and your system sees more memory. Latency is higher than native DIMMs, maybe 200-300ns instead of 100ns, but it&#39;s still memory, not storage. CXL 2.0 adds switching so multiple hosts can share a memory pool. CXL 3.0 goes further with fabric and shared memory semantics.&lt;/p&gt;
&lt;h2 id=&quot;mixing-memory-types&quot;&gt;mixing memory types&lt;/h2&gt;
&lt;p&gt;Normally your CPU&#39;s memory controller dictates what DRAM you can use. If it&#39;s a DDR5 system, all your DIMMs have to be DDR5. Same speed, same density rules, same timing specs. You can&#39;t just plug DDR4 into a DDR5 slot.&lt;/p&gt;
&lt;p&gt;CXL breaks this because the CXL device has its own memory controller, so it can use whatever DRAM it wants, DDR4, DDR5, older cheaper stuff, slower but denser modules, and the CPU doesn&#39;t care since it just sees CXL memory at some address range. So you could keep local DDR5 for hot data and use a CXL card with cheaper DDR4 as a slower tier, or use high-capacity modules that wouldn&#39;t fit your motherboard timing rules, which is nice cost-wise because you&#39;re not locked to whatever generation the motherboard supports. The tradeoff is latency since CXL adds overhead, but if you&#39;re using it for capacity expansion rather than latency-critical paths maybe that&#39;s acceptable.&lt;/p&gt;
&lt;h2 id=&quot;why-not-just-use-rdma&quot;&gt;why not just use RDMA&lt;/h2&gt;
&lt;p&gt;RDMA is a different model where you need explicit verbs, you post work requests and poll completions, it&#39;s not transparent load/store, and you have to register memory, pin pages, and exchange keys, and one-sided operations are async so you don&#39;t know when remote writes land unless you add signaling, and latency is around 1-5μs which is fast for networking but slow for memory access patterns. CXL at 200-500ns for pooled memory is closer to local DRAM territory and it&#39;s transparent to software, so your malloc can return CXL memory and the application doesn&#39;t know the difference. That&#39;s the promise anyway, the hardware shipping today is mostly local expansion cards rather than pooled memory, and the pooling stuff is still coming, hopefully.&lt;/p&gt;
&lt;h2 id=&quot;the-latency-thing&quot;&gt;the latency thing&lt;/h2&gt;
&lt;p&gt;Local DRAM is ~100ns, CXL local expansion is ~200-300ns, and CXL through a switch to a shared pool is ~500-1000ns, so pooled CXL is 5-10x slower than local, which isn&#39;t nothing, and for tight loops constantly hitting memory that seems expensive. The pitch is that it&#39;s still way better than swapping to SSD at 100μs and you get more capacity, which is true. The mental model is tiering, so hot data lives in local DRAM, warm data in the CXL pool, cold data on SSD, and the kernel or runtime migrates pages based on access patterns.&lt;/p&gt;
&lt;p&gt;Linux already has machinery for this. NUMA balancing, DAMON for access pattern detection, tiered memory support that got merged recently. Whether this works well in practice with real workloads, I don&#39;t know yet. The theory sounds reasonable but there will be edge cases.&lt;/p&gt;
&lt;h2 id=&quot;shared-memory-across-hosts&quot;&gt;shared memory across hosts&lt;/h2&gt;
&lt;p&gt;CXL 3.0 talks about multiple hosts accessing the same memory with hardware-maintained cache coherence.&lt;/p&gt;
&lt;p&gt;This sounds amazing and also scary at the same time. Cache coherence doesn&#39;t scale, the distributed shared memory people learned that in the 90s, and beyond a few nodes the coherence traffic overwhelms everything. The CXL spec people know this, so the scope is limited, maybe a rack, maybe a pod, maybe less, and the vision isn&#39;t coherent memory across the whole datacenter, it&#39;s more that within a small group of machines you can have shared memory semantics and beyond that you&#39;re back to message passing or RDMA. Even rack-scale shared memory is interesting though, since you could have databases sharing buffer caches across replicas or ML training jobs sharing model weights, so there are use cases.&lt;/p&gt;
&lt;h2 id=&quot;what-s-actually-shipping&quot;&gt;what&#39;s actually shipping&lt;/h2&gt;
&lt;p&gt;CXL 1.1 memory expanders exist today from Samsung, SK Hynix and others, and Intel Sapphire Rapids supports CXL, and these are mostly used to add capacity to memory-hungry workloads. CXL switches aren&#39;t really production-ready yet, there are some prototypes, and I&#39;d guess pooled CXL deployments are 2-3 years out. So when papers say &quot;CXL will enable this&quot; they&#39;re often talking about future hardware, and the concepts are solid but the ecosystem is young, so it&#39;s worth understanding now because it&#39;s coming, just don&#39;t expect to deploy pooled CXL next month.&lt;/p&gt;
&lt;h2 id=&quot;what-i-m-still-uncertain-about&quot;&gt;what I&#39;m still uncertain about&lt;/h2&gt;
&lt;p&gt;Latency tradeoffs are the big one, since 5-10x slower than local is real overhead, and it&#39;s better than SSD but memory-intensive applications might just thrash the CXL tier and make things worse, so the tiering policies need to actually work. Ecosystem maturity is another, because RDMA took years to get right and CXL is newer, so drivers, kernel support, allocation policies, and debugging tools all need to catch up. And it&#39;s not clear who benefits, since big cloud providers with massive memory imbalance probably see value while smaller deployments might not see ROI at current hardware costs.&lt;/p&gt;
&lt;h2 id=&quot;notes&quot;&gt;notes&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;CXL consortium includes Intel, AMD, ARM, NVIDIA, Samsung and others&lt;/li&gt;
&lt;li&gt;Built on PCIe 5.0/6.0 physical layer, same slots and cables&lt;/li&gt;
&lt;li&gt;Latency numbers vary by source and topology&lt;/li&gt;
&lt;li&gt;Linux kernel has CXL support in drivers/cxl/, device enumeration works, memory tiering is evolving&lt;/li&gt;
&lt;li&gt;Related specs: Gen-Z (seems dead), CCIX (absorbed into CXL)&lt;/li&gt;
&lt;li&gt;Good starting point: &lt;a rel=&quot;external&quot; href=&quot;https://www.computeexpresslink.org/&quot;&gt;CXL Consortium&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;For context on memory disaggregation: Aguilera et al., &quot;Memory disaggregation: why now and what are the challenges&quot;&lt;/li&gt;
&lt;/ul&gt;
</content>
        
    </entry>
</feed>
